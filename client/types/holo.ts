export interface HolocardInfo{
    cardId: number
    cardPhyscialID: string
    cardType: string
    cardName: string
    balance: number
    currentPass:string
}

export interface HoloAccountInfo{
    FirstName: string;
    LastName: string;
    PhoneNumber: string;
    Email: string;
}